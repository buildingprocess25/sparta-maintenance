---
trigger: always_on
---

1. ALWAYS read and use SKILL agents related to the context before start processing my prompt it's gitignored but you can read here: \.agents\skills

2. ALWAYS use MCP Server if needed

3. ALWAYS USE CONTEXT7 MCP SERVER if there's a library used, because it's injects real-time, version-specific documentation and code examples into LLMs, reducing hallucinations in AI-generated code

4. NO NEED TO VERIFY browser action, just tell where's neeeded tp verify then i will verify manually
