# Chore Cleanup

## Scope

Menyimpan perubahan `package.json` (penambahan `build:memory` script) dan menyimpan dokumen *implementation plan* yang baru-baru ini dibuat ke dalam sistem *version control*.

## Context and Sources

User ingin membersihkan *working directory* (git status) agar siap untuk melakukan *push* ke remote repository. 

## Changed Files

- `package.json`: Memisahkan script `build` biasa dengan script `build:memory` yang mengalokasikan 4GB memory, serta menghapus alokasi hardcoded 8GB pada perintah *default* `build`.
- `docs/plans/2026-09-04-full-pdf-photo-label-overlay.md`: Menyimpan catatan plan implementasi.

## Decisions

Semua file uncommitted langsung ditambahkan ke dalam satu commit agar *tree* bersih.

## Verification

Git status clean.

## Remaining Work and Risks

None.
