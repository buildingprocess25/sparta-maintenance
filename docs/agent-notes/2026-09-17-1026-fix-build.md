# Fix Build Error (hasWritableBranch)

## Scope

Menambahkan definisi konstan `hasWritableBranch` di dalam komponen `store-table.tsx` BMC.

## Context and Sources

- Build `npm run build:memory` sebelumnya gagal (exit code 1) karena variabel `hasWritableBranch` dipanggil di prop `disabled` pada tombol (hasil modifikasi sebelumnya), namun belum dideklarasikan di dalam file.

## Changed Files

- `app/bmc/database/_components/store-table.tsx`: Menambahkan `const hasWritableBranch = writableBranchNames.length > 0;`.

## Decisions

- Variabel langsung di-*derive* dari `writableBranchNames` agar sinkron dengan batasan cabang yang valid untuk akses saat ini.

## Verification

- `npm run build:memory` telah berhasil berjalan dengan *clean* (Compiled successfully, TypeScript checks passed).

## Remaining Work and Risks

None
