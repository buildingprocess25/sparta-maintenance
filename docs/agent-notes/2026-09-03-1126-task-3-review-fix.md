# Task 3 Review Fix

## Scope

Fixes review findings for the full-page BMC PJUM route parity task only.

## Context and Sources

- `AI_RULES.md`
- `.superpowers/sdd/task-3-brief.md`
- `.superpowers/sdd/task-3-report.md`
- `docs/agent-notes/2026-09-03-1118-full-page-bmc-pjum-route-parity.md`
- `app/reports/pjum/actions.ts`
- `app/reports/pjum/_components/pjum-view.tsx`

## Changed Files

- `app/reports/pjum/actions.ts`: adds `pjumExpiredAt: null` to the transactional report update predicate so expired reports cannot be marked exported after validation.
- `app/reports/pjum/_components/pjum-view.tsx`: moves the over-limit toast guard before the broad `canExport` return so disabled click paths can surface the limit message.
- `.superpowers/sdd/task-3-report.md`: appends this review-fix result and verification.
- `docs/agent-notes/2026-09-03-1126-task-3-review-fix.md`: records this fix.

## Decisions

- Kept the change limited to the reviewed full-page PJUM parity path.
- Left unrelated `package-lock.json` and untracked plan file untouched.

## Verification

- `npx tsx lib/pjum-selection-policy.spec.ts` failed because the local Windows
  `npx` shim points to missing
  `C:\Users\Rendi Elang\AppData\Roaming\npm\node_modules\npm\bin\npx-cli.js`.
- `node -e 'process.geteuid=()=>"codex"; require("./node_modules/tsx/dist/cjs/api/index.cjs").register(); require("./lib/pjum-selection-policy.spec.ts")'`
  passed and printed `PJUM selection policy assertions passed`.
- `node_modules/.bin/tsc.cmd --noEmit --pretty false --incremental false`
  passed with no output.

## Remaining Work and Risks

None.
