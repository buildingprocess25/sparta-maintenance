## Task 3: Export XLSX ADMIN maintenance-report list server-side

**What was done:**
- Added \rand?: StoreBrandFilter\ to \ExportFilter\ inside \pp/admin/export/queries.ts\.
- Evaluated \ilter.brand\ and pushed \getReportBrandWhere(filter.brand)\ to \where.AND\ in \uildReportWhere\ so that it scopes Report and Material sheets correctly without affecting PJUM exports.
- Added Brand select dropdown to \ExportReportsDialog\ (\pp/dashboard/reports/_components/export-reports-dialog.tsx\) to filter the reports by brand when exporting from the dashboard.
- Ensured PJUM query is unaffected as it handles brand on its own and isn't governed by \uildReportWhere\.

**Self-Review & Verification:**
- Code changes mirror the brand filtering logic applied in Task 2.
- The output column logics and layout structure are perfectly maintained.
- BMC/BNM constraints are entirely unaffected as the brand query passes into the same \where\ filter structure.
